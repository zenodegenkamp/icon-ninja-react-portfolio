import React from "react"

export default function Header(){


    return (
        <header className="header-section">
            <h1>Icon Ninja</h1>
            <p>Roll until all the icons are the same. Click each icon to freeze it at its current value between changes.</p>
        </header>
    )
}